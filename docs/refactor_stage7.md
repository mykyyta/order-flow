# Рефакторинг: Етап 7 (2026-02-07)

## Ціль етапу
Оптимізувати читання списків через `current_status`, додати логування нотифікацій і привести `telegram_bot.py` до актуальної моделі.

## План етапу
1. Перевести списки на `current_status`.
2. Додати логування для нотифікацій.
3. Виправити Telegram bot (Django setup + актуальні поля).

## Реалізація (виконано)
- Списки замовлень використовують `current_status`:
  - `orders/views.py`.
- Логування нотифікацій і таймаут для Telegram API:
  - `orders/adapters/notifications.py`, `orders/utils.py`.
- Оновлено `telegram_bot.py`:
  - налаштовано Django,
  - використано `current_status`.

## Примітки
- Логи нотифікацій пишуться через стандартний `logging`.
- `send_tg_message` тепер повертає `bool` і має таймаут.
