# Міграція OrderFlow → Pult

Мета: перейти на назву **Pult** і максимально централізувати її, щоб ім'я мало фігурувало в коді.

## Що зроблено

### 1. Централізація бренду в Django

- **Один джерело правди** для назви в UI:
  - `config/settings/base.py`: константи `SITE_NAME`, `SITE_WORDMARK`, `SITE_EMOJI` (значення за замовчуванням: Pult, PULT, 🎛️).
  - Можна перевизначити через env: `SITE_NAME`, `SITE_WORDMARK`, `SITE_EMOJI`.
- **Context processor** `config.context_processors.site_brand` передає в усі шаблони:
  - `site_name` — для заголовків сторінок (наприклад `<title>`),
  - `site_wordmark` — для логотипу в навігації,
  - `site_emoji` — для емодзі бренду.
- У шаблонах бренд не захардкоджений: використовуються `{{ site_name }}`, `{{ site_wordmark }}`, `{{ site_emoji }}`.

### 2. Перейменування Django-пакета (OrderFlow → config)

- Папка `OrderFlow/` замінена на `config/`.
- Усі посилання оновлено: `config.settings`, `config.urls`, `config.wsgi`, `config.asgi`, `config.context_processors`.
- Бренд у коді згадується лише в `config/settings/base.py` як `SITE_NAME` / `SITE_WORDMARK` / `SITE_EMOJI`.

---

## План міграції для вже розгорнутого проду

У проді вже працюють ресурси **orderflow-app**, **orderflow-migrate**, секрети **orderflow-*** і state **orderflow/prod**. Поточний код очікує **pult-app**, **pult-migrate**, **pult-*** і **pult/prod** — тому без підготовки наступний Terraform apply або Deploy призведе до помилок або дублікатів.

**Детальний покроковий план:** [docs/devops/migration_plan_prod.md](devops/migration_plan_prod.md).

Коротко:
- **Phase 1 (рекомендовано):** тимчасово повернути в коді імена orderflow-* і prefix orderflow/prod → задеплоїти новий образ на існуючий orderflow-app. Результат: Pult у проді без зміни інфраструктури.
- **Phase 2 (опційно):** створити секрети pult-*, перевести Terraform на pult/prod і нові ресурси (pult-app, pult-migrate), задеплоїти, перемкнути трафік, прибрати старі ресурси.

---

## Інфраструктура та репозиторій

Ці назви — **ідентифікатори деплою**; їхня зміна впливає на GCP, GitHub, Terraform.

| Що | Поточне значення | Примітка |
|----|------------------|----------|
| GitHub repo | `mykyyta/order-flow` | Можна перейменувати на `mykyyta/pult` в налаштуваннях репозиторію (GitHub підтримує redirect). Потім оновити змінні в CI/CD і Terraform. |
| GCP project ID | `orderflow-451220` | Зміна = новий проєкт, міграція ресурсів. Залишити рекомендовано, якщо немає потреби в новому проєкті. |
| Cloud Run service | `pult-app` | Оновлено в Terraform / workflows. |
| Cloud Run job (migrate) | `pult-migrate` | Оновлено. |
| Secret names | `pult-django-secret-key` тощо | Оновлено в Terraform; при першому apply потрібно створити нові секрети або імпорт. |
| Terraform state bucket | `orderflow-451220-tfstate` | Зазвичай не змінюють без міграції state. |

Рекомендація: спочатку оновити лише **документацію та README** на Pult; зміну назв сервісів/репо робити окремим кроком з тестами деплою.

---

## Документація

Оновлено або варто оновити на "Pult":

- `README.md` — назва проєкту та опис.
- `docs/system_overview.md` — замінити OrderFlow на Pult в описі.
- `docs/design/style_decisions.md` — замінити згадки OrderFlow на Pult де йдеться про неймінг/рефакторинг.
- `docs/implementation_roadmap.md` — те саме.
- `docs/devops/runbook.md`, `docs/devops/infrastructure_overview.md` — залишити технічні ідентифікатори (orderflow-app, orderflow-451220) як є, або оновити після зміни інфраструктури згідно з таблицею вище.

---

## Чеклист міграції

- [x] Централізація бренду: `SITE_NAME` / `SITE_WORDMARK` / `SITE_EMOJI` у settings + context processor.
- [x] Шаблони використовують `{{ site_name }}`, `{{ site_wordmark }}`, `{{ site_emoji }}`.
- [x] Перейменування пакета `OrderFlow` → `config`.
- [x] Terraform/CI: `pult-app`, `pult-migrate`, `TF_STATE_PREFIX: pult/prod`, секрети `pult-*`.
- [x] Локальна БД за замовчуванням: `pult_dev`, CI: `pult_ci`.
- [x] README та документи оновлено на Pult.
- [ ] (За бажанням) Перейменування репозиторію на GitHub на `pult` та оновлення `github_repository` у Terraform vars.
- [ ] Після деплою: у GitHub vars встановити/перевірити `SERVICE_NAME=pult-app`, `MIGRATE_JOB_NAME=pult-migrate` (workflows беруть з env, не з vars; вони вже в коді). Якщо використовуєте Terraform vars для цих імен — оновити vars у репозиторії.
