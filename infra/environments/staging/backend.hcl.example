# Must match pipeline: bucket from TF_STATE_BUCKET, prefix pult/staging.
# Copy to backend.hcl (gitignored) and run terraform init -reconfigure -backend-config=backend.hcl ...
bucket = "YOUR_GCP_PROJECT_ID-tfstate"
prefix = "pult/staging"
